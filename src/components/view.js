import { View, StyleSheet } from "react-native";

export const OverContentView = () => {
  return (
    <View
      style={{
        ...StyleSheet.absoluteFillObject,
        backgroundColor: "black",
      }}
    />
  );
};
